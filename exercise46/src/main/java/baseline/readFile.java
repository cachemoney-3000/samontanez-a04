package baseline;

import java.util.ArrayList;

public class readFile {
    //a method that will read the contents of the input file
    public ArrayList<String> read(ArrayList<String> list) {
        //will catch an exception error
        try {
            //keep looping until there are no lines left
            while () {
                //add the contents of the input file into an arraylist
            }
        } catch (Exception e) {
            System.out.println(e);
        }
        return list;
    }
}

